import { ViewPlaceholder } from "@/components/ViewPlaceholder";

export default function DesignView() {
  return (
    <ViewPlaceholder title="Director Layer" module="Module 3 — Design">
      Three-pane stage: shot navigator · Frame / Floor Plan / Light tabs · live
      compiled Prompt Panel. Shot size ladder, lens picker with FOV preview,
      composition overlays, 180°-rule enforcement, and lighting designer.
    </ViewPlaceholder>
  );
}
