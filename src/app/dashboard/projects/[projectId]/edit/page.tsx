import { ViewPlaceholder } from "@/components/ViewPlaceholder";

export default function EditView() {
  return (
    <ViewPlaceholder title="Timeline & Post" module="Module 12 — Editing & Post">
      Multi-track timeline with AI-suggested cut points, in-place retake, project
      grade/LUT, captions, and export to MP4 4K plus FCPXML / OTIO / EDL handoff.
    </ViewPlaceholder>
  );
}
