import { ViewPlaceholder } from "@/components/ViewPlaceholder";

export default function CanvasView() {
  return (
    <ViewPlaceholder title="Node Canvas" module="Module 5 — Canvas & Workflow">
      Infinite node canvas: prompts, images, videos, entities, skills, and
      enhance operations as connectable, re-runnable nodes. Pipeline templates
      power Quick Apps.
    </ViewPlaceholder>
  );
}
