import { ViewPlaceholder } from "@/components/ViewPlaceholder";

export default function BoardView() {
  return (
    <ViewPlaceholder title="Storyboard" module="Module 3 — Storyboard">
      Script-to-storyboard with entity injection, 9-frame scene templates, and
      drag-reorder. Frames compile into the Director Layer for shot design.
    </ViewPlaceholder>
  );
}
